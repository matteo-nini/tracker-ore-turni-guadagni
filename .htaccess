# ✨ CREA un file .htaccess nella root del progetto con questo contenuto:

# Disabilita cache per file dinamici
<FilesMatch "\.(php|json|csv|txt)$">
    Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
    Header set Pragma "no-cache"
    Header set Expires "0"
</FilesMatch>

# Cache limitata per CSS e JS (1 ora)
<FilesMatch "\.(css|js)$">
    Header set Cache-Control "max-age=3600, must-revalidate"
</FilesMatch>

# Proteggi cartelle sensibili
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Blocca accesso diretto ai file di log
    RewriteRule ^calendar/changes_log\.txt$ - [F,L]
    
    # Blocca accesso diretto a profiles.json
    RewriteRule ^profiles/profiles\.json$ - [F,L]
</IfModule>

# Headers di sicurezza
<IfModule mod_headers.c>
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
</IfModule>

# Abilita compressione GZIP per migliorare performance
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>